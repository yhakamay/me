# GitHub personal access token (classic or fine-grained) used to fetch the
# public repository list with a higher rate limit. Optional: without it the
# site still builds, falling back to unauthenticated requests (lower limit).
# Create one at https://github.com/settings/tokens
GITHUB_API_TOKEN=
